#include <iostream>
#include "robotsim.h"
using namespace std;

int main(int argc, char *argv[]) {
	CRobotSim sim;
	CMobot mobot;
	//sim.addRobot(mobot);

	//mobot.connect();
	//mobot.move(0, 45, -45, 0);

	return 0;
}
